<h1>Add a new article</h1>

<%= form_with model: @article do |t| %>
<%= t.label :title %>
<%= t.text_field :title %>
<%= t.label :content %>
<%= t.text_field :content %>
<%= t.submit "add article" %>
<% end %>

<%= link_to "Back to all articles", articles_path %>
